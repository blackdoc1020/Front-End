5. 가상 요소 선택자

ABC::before :BEFORE, 인라인(글자) 요소, 가상 요소 선택자(Pseudo-Elements) 
선택자 ABC 요소의 내부 앞에 내용(Content)을 삽입.
ex)
.box::before{
    content: "앞!";
}

ABC::after  :AFTER, 인라인(글자) 요소, 가상 요소 선택자(Pseudo-Elements) 
선택자 ABC 요소의 내부 뒤에 내용(Content)을 삽입.
ex)
.box::after{
    content: "뒤!";
}